# Endpoint Index

Status: Current
Source: `app/backend/main.py` (this project has no FastAPI backend — index maintained for future API layer)

This file is checked by `scripts/check_docs_integrity.py`. Keep in sync with any future `app/backend/main.py` routes.

## Current Endpoints

_No FastAPI backend in v1. The pipeline runs as a script (`run_monthly.py`)._
_This file is a placeholder — populate when an API layer is added._

<!-- When adding routes, use exactly this format (one route per line):
- `GET /runs`
- `POST /runs`
- `GET /runs/{run_id}`
-->
